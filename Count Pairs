int countAffordablePairs(vector<int> arr, int target) {
 vector< pair<int,int> > pairs;
 int n=arr.size();
 
 for(int i=0;i<n-1;i++){
    for(int j=i+1;j<n;j++){
        if(arr[i]+arr[j]<=target){
            pairs.push_back({arr[i],arr[j]});
        }
    }
 }
 return pairs.size();
}
